require "nvchad.options"

-- add yours here!

-- local o = vim.o
-- o.cursorlineopt ='both' -- to enable cursorline!

-- rounded border on all default floating windows (hover, signature help,
-- diagnostics) instead of the borderless default, which blends into text
vim.o.winborder = "rounded"
